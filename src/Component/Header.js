import React from 'react';
import logo from './assets/logo.webp'

const Header = () => {
    return ( 
        <header>
            <img src={logo} alt='log' />
        </header>
    );
}
 
export default Header;